{
  "name": "save-the-world-idle",
  "version": "0.5.0",
  "private": true,
  "description": "Pixel-art idle game: run through a broken world and help it heal. Single file, no build.",
  "scripts": {
    "test": "node test/smoke.js",
    "start": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
